backend:
  name: github
  repo: wizzidom/armofgod3.0
  branch: main
  base_url: https://sveltia-cms-auth.majolawisdom.workers.dev

media_folder: "assets/gallery"
public_folder: "/assets/gallery"

# Show a link to the live site after publishing
show_preview_links: true

# Return to the collection list after saving
editor:
  preview: false

collections:
  - name: "gallery"
    label: "Gallery"
    label_singular: "Photo"
    folder: "_data/photos"
    format: "json"
    create: true
    delete: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    summary: "{{title}} — {{category}}"
    # Go back to list after saving
    editor:
      preview: false
    fields:
      - label: "Image"
        name: "src"
        widget: "image"
        hint: "Upload a photo from your device. It will be saved to /assets/gallery/"

      - label: "Title"
        name: "title"
        widget: "string"
        hint: "Short title shown on hover (e.g. Sunday Morning Worship)"

      - label: "Category"
        name: "category"
        widget: "select"
        options:
          - { label: "Worship",         value: "worship" }
          - { label: "Sunday Services", value: "sunday" }
          - { label: "Community",       value: "community" }
          - { label: "Youth",           value: "youth" }
          - { label: "Outreach",        value: "outreach" }
          - { label: "Conferences",     value: "conferences" }
        hint: "Choose the category that best fits this photo"

      - label: "Description"
        name: "description"
        widget: "string"
        required: false
        hint: "Optional caption shown when the photo is opened in the lightbox"
